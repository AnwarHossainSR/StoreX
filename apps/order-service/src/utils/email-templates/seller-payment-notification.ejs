<!-- Create this as: seller-payment-notification.ejs -->
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Payment Received - StoreX</title>
    <style>
      body {
        margin: 0;
        padding: 0;
        font-family: "Helvetica Neue", Arial, sans-serif;
        background-color: #f5f7fa;
      }
      .container {
        max-width: 600px;
        margin: 30px auto;
        background: white;
        border-radius: 12px;
        overflow: hidden;
        box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
      }
      .header {
        background: linear-gradient(135deg, #059669, #10b981);
        padding: 40px 30px;
        text-align: center;
        color: white;
      }
      .header h1 {
        margin: 0;
        font-size: 24px;
        font-weight: 500;
      }
      .content {
        padding: 40px;
      }
      .payment-info {
        background: #f0fdf4;
        padding: 20px;
        border-radius: 8px;
        margin: 20px 0;
        border-left: 4px solid #10b981;
      }
      .amount-highlight {
        background: #059669;
        color: white;
        padding: 20px;
        border-radius: 8px;
        text-align: center;
        margin: 20px 0;
      }
      .amount-highlight .amount {
        font-size: 28px;
        font-weight: 700;
        margin: 10px 0;
      }
      .cta-button {
        display: inline-block;
        background: linear-gradient(135deg, #059669, #10b981);
        color: white;
        padding: 15px 30px;
        text-decoration: none;
        border-radius: 8px;
        font-weight: 600;
        margin: 20px 0;
      }
      .footer {
        background: #f8fafc;
        padding: 25px;
        text-align: center;
        font-size: 13px;
        color: #6b7280;
        border-top: 1px solid #e2e8f0;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <h1>💰 Payment Received!</h1>
        <p>Your payment has been processed</p>
      </div>
      <div class="content">
        <h2>Hello <%= sellerName %>,</h2>
        <p>
          Excellent news! Payment for your recent order has been successfully
          transferred to your account.
        </p>

        <div class="amount-highlight">
          <div>Amount Transferred</div>
          <div class="amount">$<%= netAmount.toFixed(2) %></div>
        </div>

        <div class="payment-info">
          <h3 style="margin: 0 0 15px 0; color: #1f2a44">Payment Breakdown</h3>
          <div><strong>Order ID:</strong> #<%= orderId %></div>
          <div>
            <strong>Total Order Amount:</strong> $<%= paymentAmount.toFixed(2)
            %>
          </div>
          <div>
            <strong>Platform Fee (5%):</strong> -$<%= platformFee.toFixed(2) %>
          </div>
          <div><strong>Net Amount:</strong> $<%= netAmount.toFixed(2) %></div>
          <div><strong>Payment Date:</strong> <%= paymentDate %></div>
        </div>

        <p>
          The funds should appear in your connected bank account within 2-7
          business days.
        </p>

        <div style="text-align: center">
          <a href="<%= dashboardUrl %>" class="cta-button"
            >View Payment Details</a
          >
        </div>
      </div>
      <div class="footer">
        <p>© 2025 StoreX. All rights reserved.</p>
      </div>
    </div>
  </body>
</html>
